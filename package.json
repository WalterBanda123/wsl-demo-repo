{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "start": "nodemon app.js",
    "build": "npm run lint && npm run copy-files",
    "lint": "echo 'Running linter...' && exit 0",
    "copy-files": "echo 'Copying files...' && mkdir -p build && cp app.js build/"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "http-status-codes": "^2.3.0",
    "jest": "^29.7.0",
    "mongoose": "^8.4.3",
    "nodemon": "^3.1.4"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
